


// Данный компонент отвечает за отрисовку корневого компонента
// по сути того - что будет в main после захода на сайт

const FirstPage = () => {
    return (<p> Это первая страница - так сказать, корневой main</p>)
};

export default FirstPage;